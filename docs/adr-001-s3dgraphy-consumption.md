# ADR-001 — How non-Python tools consume s3Dgraphy

Status: **accepted** (E. Demetrescu, 12 July 2026).
Scope: EMStudio, Heriverse and any future non-Python consumer of the EM
language. s3Dgraphy remains the source of truth of the whole EM ecosystem:
change it, and every derived tool must follow.

## Problem

s3Dgraphy carries two different kinds of truth:

1. **The language** (declarative): node types and their hierarchy, edge
   types and connection rules, visual rules, palette icons, CIDOC mappings.
   Historically split between the `JSON_config` datamodels *and* the Python
   class definitions (the type hierarchy lived only in code).
2. **The behaviour** (algorithmic): GraphML/Excel import, validation logic,
   export to RDF, graph operations.

Web tools cannot run Python. EMStudio initially bridged the gap by *mining*
the class hierarchy via introspection into a private JSON — pragmatic, but
it silently forked the truth. Heriverse's developers proposed translating
s3dgraphy to JavaScript. A conscious, shared decision was needed.

## Options considered

### A. JSON contract + explicit sync; shared Rust core (WASM) at maturity — CHOSEN

The datamodels in `JSON_config` are the *complete* contract of the language,
including the class hierarchy (now in `s3Dgraphy_node_datamodel.json`
itself, per-entry `parent`/`node_type` fields — E.D.'s call: "the hierarchy
belongs in the two datamodel files"). A tool inside s3dgraphy
(`python -m s3dgraphy.tools.sync_node_datamodel`) regenerates those fields
from the classes and a test guards against drift. Consumers copy the JSONs
with a declared, versioned sync step (EMStudio:
`frontend/scripts/sync-datamodels.sh`) and never edit the copies.
Behavioural logic follows the roadmap already chosen for EMStudio: em-core
(Rust) implements the editing/validation semantics, compiled to WASM for
web consumers (Heriverse included, if they wish), validated against the
Python reference with shared fixtures (e.g. TempluMare golden files).

* **Pro** — single declarative truth, versioned with the datamodel; sync is
  visible in diffs (no silent forks); zero heavy runtime in the browser; at
  maturity, *one* runtime implementation for all web tools instead of N;
  Python stays the reference implementation, and pyArchInit/EM-blender-tools
  are untouched.
* **Con** — behaviour exists twice (Python reference + Rust runtime) until
  parity tests are strong; requires discipline (semantic changes start from
  the datamodel + fixtures, then implementations follow); small upfront
  cost (the sync tooling — already done).

### B. JSON contract only

Same as A but every tool re-implements behaviour ad hoc from the JSONs.

* **Pro** — simplest, no extra toolchain.
* **Con** — as semantics grow (epoch propagation, composite validation,
  reasoning), each language grows its own divergent logic. Fine as step 1;
  insufficient alone. (A includes B as its first stage.)

### C. JavaScript port of s3dgraphy

* **Pro** — Heriverse devs work in their language; no WASM toolchain;
  direct browser access.
* **Con** — a second *complete* implementation maintained by hand: drift is
  not a risk but a certainty; double bugfixing; ports are never complete
  (GraphML/Excel importers, RDF export…); over time the de-facto truth
  migrates to whichever port has more users. Rejected.

### D. Python in the browser (Pyodide/WASM)

* **Pro** — literally one implementation, zero drift.
* **Con** — ~10–15 MB runtime, seconds of cold start, awkward JS interop
  for a 60 fps interactive editor; absurd inside Tauri (native Rust/Python
  available). Reasonable for notebook-style analysis pages only. Rejected
  for editors.

## Decision

**Option A.** Concretely:

1. `s3Dgraphy_node_datamodel.json` now carries the full class registry
   (42 classes, `parent` + `node_type` per entry), regenerated by
   `s3dgraphy.tools.sync_node_datamodel`; `tests/test_node_datamodel_registry.py`
   fails CI when the JSON and the classes diverge.
2. EMStudio consumes only the official JSONs (`scripts/sync-datamodels.sh`,
   copies pinned in `src/assets/`, never hand-edited). The private
   `em_node_classes.json` mining artefact was removed.
3. Behavioural semantics land in em-core (Rust) with WASM delivery for web
   consumers; the Python implementation remains the reference, with shared
   fixtures as the parity contract.
4. Heriverse: same JSONs today; em-core WASM as an option when it ships.

## Open datamodel issues found while implementing

* `allowed_connections` reference `VirtualStratigraphicUnit`, but no such
  class exists (USVn/USVs descend from `NonStructuralVirtualStratigraphicUnit` /
  `StructuralVirtualStratigraphicUnit` directly under `StratigraphicNode`).
  EMStudio applies a suffix alias as a workaround; upstream curation should
  either add the intermediate class or list the concrete ones.
* `RSF` (`ReusedSpecialFind`) does not descend from `SpecialFindUnit`, so
  rules targeting `SpecialFindUnit` exclude it. Verify intent.
