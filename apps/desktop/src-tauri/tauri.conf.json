{
  "$schema": "https://schema.tauri.app/config/2",
  "productName": "EMStudio",
  "version": "1.6.0",
  "identifier": "org.extendedmatrix.emstudio",
  "build": {
    "frontendDist": "../../../frontend/dist",
    "devUrl": "http://localhost:5173",
    "beforeDevCommand": {
      "script": "npm run dev",
      "cwd": "../../../frontend"
    },
    "beforeBuildCommand": {
      "script": "npm run build",
      "cwd": "../../../frontend"
    }
  },
  "app": {
    "windows": [
      {
        "title": "EMStudio",
        "width": 1440,
        "height": 920,
        "minWidth": 800,
        "minHeight": 500
      }
    ],
    "security": {
      "csp": null
    }
  },
  "bundle": {
    "active": true,
    "targets": "all",
    "externalBin": [
      "binaries/em-bridge"
    ],
    "icon": [
      "icons/32x32.png",
      "icons/128x128.png",
      "icons/128x128@2x.png",
      "icons/icon.icns",
      "icons/icon.ico"
    ],
    "fileAssociations": [
      {
        "ext": [
          "emj"
        ],
        "name": "Extended Matrix document",
        "description": "Extended Matrix graph (em.json)",
        "role": "Editor"
      }
    ]
  },
  "plugins": {
    "deep-link": {
      "//": "THE HANDOFF SCHEME. `stratigraph://open?server=&room=` is the ecosystem's, not this app's: the Catalog opens a STUDY on it and StratiGraph Server's room browser opens a ROOM (see stratigraph-server/app/handoff.py). Registering it here is what makes 'open in EMStudio' a click instead of three fields and a pasted token. The link carries NO credential — the app signs in by itself (frontend/src/oidc.ts). | Moved here from `bundle.deepLinkProtocols` on 2026-09-16: that key is not in the Tauri 2 schema and `bundle` refuses what it does not know, which is what failed the four release builds. The schemes belong to tauri-plugin-deep-link (Cargo.toml), so this is where they are read. NB the plugin registers them on Windows and Linux; on macOS the scheme reaches the app through CFBundleURLTypes in Info.plist — TEST THE HANDOFF ON macOS before calling it intact.",
      "desktop": {
        "schemes": [
          "stratigraph"
        ]
      }
    }
  }
}
